{%- style -%}
  .row {
    display: flex;
    justify-content: space-between;
    padding: 4rem 0 10rem 0;
  }

  .col-logo {
    flex: 20%;
    text-align: right;
    padding-right: 50px;
  }

  .col-info {
    flex: 20%;
  }

  .col-contact {
    flex: 20%;
  }

  .col-join_us {
    /* flex: 10%; */
    width: 357px;
  }

  .col-head {
    font-size: 24px;
    font-weight: 700;
    color: #333;
  }

  .list {
    list-style: none;
    padding: 0;
  }

  .list-item {
    text-decoration: none;
    font-size: 16px;
    font-weight: 400;
    color: #333;
  }

  .group {
    display: flex;
    justify-content: space-between;
  }

  .group label {
    display: block;
  }

  .content {
    margin-left: 40px;
  }

  .content > p {
    margin: 0;
    font-size: 16px;
    font-weight: 700;
    color: #004289;
  }

  .contact-wrapper {
    margin: 10px 0;
  }

  .contact-container {
    width: 50%;
  }

  .others {
    margin: 20px 0;
  }

  .others > P {
    margin: 0;
  }

  .footer__list-social {
    margin: 0;
  }

  .footer__list-social > li {
    background-color: #004289;
    margin-right: 10px;
  }

  .footer__list-social > li > a {
    padding: 0;
  }

  .news-letter {
    padding: 40px;
    background-color: #004289;
    /* height: 100%; */
  }

  .form-header-text {
    text-transform: capitalize;
    font-size: 24px;
    font-weight: 700;
    color: #fff;
  }

  .form-header-text > p,
  .form-header-text {
    margin-top: 0;
  }

  .form-body-text {
    font-size: 16px;
    font-weight: 400;
    color: #ccd1ce;
    line-height: 25.6px;
    margin: 20px 0;
  }

  .white-edit {
    width: 100%;
    padding: 20px;
    border: none;
  }

  .white-edit:focus-visible {
    outline: none;
    box-shadow: none;
  }

  .submit-btn {
    width: 100%;
    padding: 15px 0;
    background-color: #032c58;
    border: none;
    cursor: pointer;
    text-transform: uppercase;
    font-size: 16px;
    color: white;
    font-weight: 700;
    margin-top: 20px;
    letter-spacing: 1.3px;
    font-family: 'IBM Plex Sans';
  }

  .email-wrapper > label {
    color: white;
  }

  .policy {
    display: flex;
    justify-content: space-around;
    padding: 50px 150px;
    border-top: solid 1px #004289;
  }

  .links-wrapper > ul {
    display: flex;
    justify-content: space-around;
    list-style: none;
    margin: 0;
  }

  .links-wrapper > ul a {
    padding: 0 10px;
    text-decoration: none;
    font-size: 16px;
    color: #333;
    font-weight: 400;
  }

  .logo-img {
    display: block;
    object-fit: cover;
  }

  .social-group {
    text-align: left;
  }

  .col > h2 {
    margin-top: 0;
  }

  @media screen and (min-width: 750px) and (max-width: 989px) {
    .social-group {
      width: 70%;
    }

    .footer__list-social > li {
      width: 50px;
      margin: 10px;
    }

    .row {
      display: block !important;
      width: 70%;
      margin: 0 auto;
    }

    .col {
      text-align: center;
    }

    .content-wrapper {
      text-align: center;
      display: inline-block;
    }

    .social-group {
      display: contents;
    }

    .col-join_us {
      width: 80%;
      display: inline;
    }

    .contact-container {
      display: inline-block;
    }

    .custom-align {
      justify-content: center;
    }

    .group {
      justify-content: space-around;
    }

    .content {
      margin: 0;
    }

    .col-logo {
      padding-right: 0;
    }

    .swiper-wrapper {
      right: -240px;
    }
  }

  @media screen and (max-width: 749px) {
    .social-group {
      width: 70%;
    }

    .footer__list-social > li {
      width: 50px;
      margin: 10px;
    }

    .row {
      display: block !important;
      width: 90%;
      margin: 0 auto;
    }

    .col {
      text-align: center;
    }

    .content-wrapper {
      text-align: center;
      display: inline-block;
    }

    .social-group {
      display: contents;
    }

    .col-join_us {
      width: 80%;
      display: inline;
    }

    .contact-container {
      display: inline-block;
    }

    .custom-align {
      justify-content: center;
    }

    .group {
      justify-content: space-around;
    }

    .content {
      margin-left: 1rem;
      margin-right: 0;
    }

    .col-logo {
      padding-right: 0;
    }

    .swiper-wrapper {
      right: -240px;
    }

    .links-wrapper > ul a {
      font-size: 10px;
    }

    .policy {
      display: block;
      padding: 0;
      text-align: center;
    }

    .logo-img {
      display: inline;
    }
  }
{%- endstyle -%}

<div class="row">
  {%- liquid
    assign has_social_icons = true
    if settings.social_facebook_link == blank and settings.social_instagram_link == blank and settings.social_youtube_link == blank and settings.social_tiktok_link == blank and settings.social_twitter_link == blank and settings.social_pinterest_link == blank and settings.social_snapchat_link == blank and settings.social_tumblr_link == blank and settings.social_vimeo_link == blank
      assign has_social_icons = false
    endif
  -%}
  <div class="col col-logo">
    {%- if settings.logo != blank -%}
      {% comment %} <img src="{{ section.settings.image | image_url }}" class="img-logo" width="20" height="20"> {% endcomment %}
      {%- assign logo_alt = settings.logo.alt | default: shop.name | escape -%}
      {%- assign logo_height = settings.logo_width | divided_by: settings.logo.aspect_ratio -%}
      {% capture sizes %}(min-width: 750px) {{ settings.logo_width }}px, 50vw{% endcapture %}
      {% capture widths %}{{ settings.logo_width }}, {{ settings.logo_width | times: 1.5 | round }}, {{ settings.logo_width | times: 2 }}{% endcapture %}
      {{
        settings.logo
        | image_url: width: 600
        | image_tag:
          class: 'header__heading-logo',
          widths: widths,
          height: logo_height,
          width: settings.logo_width,
          alt: logo_alt,
          sizes: sizes,
          preload: true
      }}
    {%- endif -%}
  </div>
  {%- for block in section.blocks -%}
    {%- case block.type -%}
      {%- when 'info' -%}
        <div class="col col-{{ block.type }}">
          <h2 class="col-head">{{ block.settings.heading_text }}</h2>
          {%- if block.settings.menu != blank -%}
            <ul class="list">
              {%- for item in block.settings.menu.links -%}
                <li class="list-item">
                  <a href="{{ item.url }}" class="list-item">
                    {{ item.title }}
                  </a>
                </li>
              {%- endfor -%}
            </ul>
          {%- endif -%}
        </div>
      {%- when 'contact' -%}
        <div class="col col-{{ block.type }}">
          <h2 class="col-head">{{ block.settings.heading }}</h2>
          <div class="contact-wrapper">
            <div class="contact-container">
              <div class="group">
                <div class="label">
                  <label>Tel</label>
                  <label>Email</label>
                </div>
                <div class="content">
                  <p>{{ block.settings.tel }}</p>
                  <p>{{ block.settings.email }}</p>
                </div>
              </div>
              <div class="others">
                <p>{{ block.settings.company_info }}</p>
                <p>{{ block.settings.name }}</p>
                <p>{{ block.settings.country }}</p>
              </div>
              <p>Follow us</p>
            </div>
            <div class="social-group">
              {%- if block.settings.manage_social and has_social_icons -%}
                {%- render 'social-icons', class: 'footer__list-social custom-align' -%}
              {%- endif -%}
            </div>
          </div>
        </div>
      {%- when 'join_us' -%}
        <div class="col col-{{ block.type }}">
          <form class="news-letter">
            <div class="form-conayiner">
              <h2 class="form-header-text">{{ block.settings.join_heading_text }}</h2>
              <p class="form-body-text">{{ block.settings.join_body_text }}</p>
              <div class="email-wrapper">
                <label>Email Address</label>
                <input type="email" class="white-edit" row="3">
                <input type="submit" class="submit-btn" value="{{ block.settings.btn_text }}">
              </div>
            </div>
          </form>
        </div>
    {%- endcase -%}
  {%- endfor -%}
</div>
<div class="policy">
  <div class="links-wrapper">
    <ul>
      <li>
        <a href="{{ section.settings.policy_link }}">{{ section.settings.policy }}</a><span>|</span>
      </li>
      <li>
        <a href="{{ section.settings.cookies_link }}">{{ section.settings.cookies }}</a><span>|</span>
      </li>
      <li>
        <a href="{{ section.settings.text_link }}">{{ section.settings.text }}</a>
      </li>
    </ul>
  </div>
  {%- if section.settings.company_logo != blank -%}
    <div class="logo-wrapper">
      <img class="logo-img" src="{{ section.settings.company_logo | image_url: medium }}">
    </div>
  {%- endif -%}
</div>
{% schema %}
{
  "name": "custom-footer",
  "tag": "footer",
  "class": "footer",
  "settings": [
    {
      "type": "text",
      "id": "policy",
      "label": "Policy Text",
      "default": "Copyright @ StudioForty9"
    },
    {
      "type": "url",
      "id": "policy_link",
      "label": "Policy Link"
    },
    {
      "type": "text",
      "id": "cookies",
      "label": "Cookies",
      "default": "Cokkies"
    },
    {
      "type": "url",
      "id": "cookies_link",
      "label": "Cookie Link"
    },
    {
      "type": "text",
      "id": "text",
      "label": "Text",
      "default": "Terms & Conditions"
    },
    {
      "type": "url",
      "id": "text_link",
      "label": "Text Link"
    },
    {
      "type": "image_picker",
      "id": "company_logo",
      "label": "Company Logo"
    }
  ],
  "blocks": [
    {
      "type": "info",
      "name": "Info",
      "settings": [
        {
          "type": "text",
          "id": "heading_text",
          "label": "Heading Text",
          "default": "Info"
        },
        {
          "type": "link_list",
          "id": "menu",
          "label": "Menu"
        }
      ]
    },
    {
      "type": "contact",
      "name": "Contact",
      "settings": [
        {
          "type": "text",
          "id": "heading",
          "label": "Heading text",
          "default": "Contact"
        },
        {
          "type": "text",
          "id": "tel",
          "label": "Telephone",
          "default": "012 3456789"
        },
        {
          "type": "text",
          "id": "email",
          "label": "Email",
          "default": "hello@domain.com"
        },
        {
          "type": "text",
          "id": "company_info",
          "label": "Company info",
          "default": "Lorem Ipsum Outdoor "
        },
        {
          "type": "text",
          "id": "name",
          "label": "Name",
          "default": "Unit 1, Lipsum,"
        },
        {
          "type": "text",
          "id": "country",
          "label": "Country",
          "default": "City, Co. County"
        },
        {
          "type": "checkbox",
          "id": "manage_social",
          "label": "Manage Social networks",
          "default": true
        }
      ]
    },
    {
      "type": "join_us",
      "name": "Join us",
      "settings": [
        {
          "type": "color",
          "id": "bk_color",
          "label": "Background Color",
          "default": "#004289"
        },
        {
          "type": "richtext",
          "id": "join_heading_text",
          "label": "Head Text",
          "default": "<p>join our Newsletter</p>"
        },
        {
          "type": "text",
          "id": "join_body_text",
          "label": "Body Text",
          "default": "Be the first to know about Our offers, events, new arrivals & sales."
        },
        {
          "type": "text",
          "id": "btn_text",
          "label": "Button Text",
          "default": "Sign Up"
        },
        {
          "type": "color",
          "id": "btn_color",
          "label": "Button Color",
          "default": "#032C58"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Custom footer",
      "blocks": [
        {
          "type": "info"
        },
        {
          "type": "contact"
        },
        {
          "type": "join_us"
        }
      ]
    }
  ]
}
{% endschema %}
